# HW07 – Report

> Файл: `homeworks/HW07/report.md`  
> Важно: не меняйте названия разделов (заголовков). Заполняйте текстом и/или вставляйте результаты.

## 1. Datasets

Вы выбрали 3 датасета из 4 (перечислите):

1. **S07-hw-dataset-01**
2. **S07-hw-dataset-02**
3. **S07-hw-dataset-03**

### 1.1 Dataset A

- Файл:  S07-hw-dataset-01.csv
- Размер: 12000, 9 (строк, столбцов)
- Признаки: числовые (int64, float64)
- Пропуски: нет
- "Подлости" датасета: числовые признаки в разных шкалах + шум

### 1.2 Dataset B

- Файл:  S07-hw-dataset-02.csv
- Размер: 8000, 4 (строк, столбцов)
- Признаки: числовые (int64, float64)
- Пропуски: нет
- "Подлости" датасета: Нелинейная структура (вложенные круги/луны) + выбросы

### 1.3 Dataset C

- Файл:  S07-hw-dataset-03.csv
- Размер: 15000, 5 (строк, столбцов)
- Признаки: числовые (int64, float64)
- Пропуски: нет
- "Подлости" датасета: Кластеры разной плотности + фон

## 2. Protocol

Опишите ваш "честный" unsupervised-протокол.

- Препроцессинг: что именно делали (scaling, imputation, encoding, PCA – если делали)
  - **Scaling:** Использовался `StandardScaler` для всех датасетов. 
  - **Imputation:** `SimpleImputer(strategy='median')` (хотя явных пропусков не обнаружено, добавлено для надежности пайплайна).
  - **Encoding:** Категориальные признаки отсутствовали, кодирование не потребовалось.
- Поиск гиперпараметров:
  - **KMeans:** Перебор `k` в диапазоне от 2 до 10.
  - **DBSCAN:** GridSearch по `eps` [0.1, 0.3, 0.5, ..., 1.5] и `min_samples` [3, 5, 10].
  - **Критерий выбора:** Максимизация метрики `silhouette_score`.
- Метрики: silhouette / Davies-Bouldin / Calinski-Harabasz (и как считали для DBSCAN при наличии шума)
  - Рассчитывались Silhouette Score, Davies-Bouldin, Calinski-Harabasz
  - Метрики считались только для точек, не помеченных как шум (`label != -1`). Также фиксировалась доля шума (`noise_ratio`).
- Визуализация: PCA(2D) (и t-SNE, если делали – с какими параметрами)
  - PCA (2D) для отображения итогового разбиения.
  - Графики зависимости Silhouette от гиперпараметров (`k` или `eps`)

## 3. Models

Перечислите, какие модели сравнивали **на каждом датасете**, и какие параметры подбирали.

Минимум (для каждого датасета):

- KMeans (поиск `k`, фиксировали `random_state`, `n_init`)
- Один из:
  - DBSCAN (`eps`, `min_samples`, доля шума), или
  - AgglomerativeClustering (`k`, `linkage`)

Для каждого датасета сравнивались два алгоритма:

1. **KMeans:**
    - Параметры: `n_clusters` (подбирался), `random_state=42`, `n_init=10`
2. **DBSCAN:**
    - Параметры: `eps` (радиус окрестности), `min_samples` (плотность)

## 4. Results

Для каждого датасета – краткая сводка результатов.

### 4.1 Dataset A

- Лучший метод и параметры: KMeans, `n_clusters=2`
- Метрики (silhouette / DB / CH):
  - Silhouette: 0.522
  - Davies-Bouldin: 0.685
  - Calinski-Harabasz: 11786.95
- Коротко: Датасет имеет структуру небольших сгустков. KMeans показал себя лучше. DBSCAN проиграл по метрике (0.397), так как на таких кластерах KMeans часто дает более плотное разбиение.

### 4.2 Dataset B

- Лучший метод и параметры: DBSCAN, `eps=0.1`, `min_samples=10`
- Метрики (silhouette / DB / CH):
  - Silhouette: 0.581
  - Davies-Bouldin: 0.578
  - Доля шума: **91.85%**
- Коротко: Формально DBSCAN выиграл у KMeans, так как данные имеют нелинейную структуру. Однако, автоматический выбор параметров привел к `eps=0.1`, что слишком мало — 91% данных ушло в шум.

### 4.3 Dataset C

- Лучший метод и параметры: DBSCAN, `eps=0.1`, `min_samples=10`
- Метрики (silhouette / DB / CH):
  - Silhouette: 0.812
  - Davies-Bouldin: 0.245
  - Доля шума: **99.59%**
- Коротко: Аналогичная ситуация с датасетом B, но еще хуже

## 5. Analysis

### 5.1 Сравнение алгоритмов (важные наблюдения)

- Где KMeans "ломается" и почему? В моем случае на нелинейных, сложной формы (которыые, например, визуально не поделены на отдельные островки) датасетах 2 и 3 датасетах
- Где DBSCAN/иерархическая кластеризация выигрывают и почему? На данных сложной формы. Он способен игнорировать выбросы. Однако эксперимент показал уязвимость метрики Silhouette для DBSCAN: алгоритм может "считерить", оставив только самые лучшие точки и отбросив 99% данных в шум, получив формально высокий результат.
- Что сильнее всего влияло на результат (масштабирование, выбросы, плотность, пропуски, категориальные признаки)? масштабирование и выбросы

### 5.2 Устойчивость (обязательно для одного датасета)

- Какую проверку устойчивости делали (5 запусков KMeans по разным seed или иной подход)
- Что получилось (в 3-6 строк)
- Вывод: устойчиво/неустойчиво и почему вы так считаете

- **Проверка:** 5 запусков KMeans с разными `random_state` (seeds).
- **Метрика:** Adjusted Rand Index (ARI) между первым запуском и последующими.
- **Результат:** Средний ARI = **1.0000**.
- **Вывод:** Кластеризация абсолютно устойчива. Это говорит о том, что кластеры в 01 хорошо разделены

### 5.3 Интерпретация кластеров

- Как вы интерпретировали кластеры:
  - профили признаков (средние/медианы) **или**
  - любая другая логичная интерпретация
- 3-6 строк выводов

 Для **01** (KMeans) разбиение прошло по значениям признаков. Кластеры представляют собой группы объектов с разным уровнем выраженности признаков (например, "низкие значения", "высокие значения").  
 Для **02/03** (DBSCAN) интерпретация затруднена из-за экстремально высокой доли шума (>90%). Алгоритм выделил лишь самые плотные ядра распределений, проигнорировав периферию кластеров.

## 6. Conclusion

4-8 коротких тезисов: чему научились про кластеризацию, метрики и корректный протокол unsupervised-эксперимента.

1. Выбор лучшей модели только по `Silhouette Score` привел к выбору настроек DBSCAN, которые отбрасывают 90-99% данных в шум. Необходимо вводить штраф за долю шума или ставить порог (например, шум < 20%)
2. KMeans надежен, но прост. Он дает гарантированный результат на всех данных, но плохо работает с нелинейной геометрией
3. Важность визуализации. PCA-графики позволили увидеть, что "победа" DBSCAN на 03 является технической (из-за удаления почти всех точек), а не содержательной
4. На хорошо разделенных данных DS-01 KMeans работает отлично (ARI=1.0)
